#include "SquareMatrix.hpp"

template <typename T>
SquareMatrix<T>::SquareMatrix(int n) : Matrix<T> {

}


template <typename T>
SquareMatrix<T> load(const char *filename) {

}

template <typename T>
SquareMatrix<T> operator=(const SquareMatrix<T>& m) {
    
}